**/__pycache__/
*.pyc
backend/.venv/
backend/.venv.broken_*/
backend/.env
.env

frontend/**/node_modules/

# 案件資料夾與通聯歷程檔案（含個資與偵查機密，絕不上傳）
基地台位置範例檔案/
*.xlsx
*.xltx
*.xlsm
*.xltm
*.csv
*.tsv
*.pdf

# 地圖圖磚與上傳暫存
data/tiles/*
data/uploads/*

# 圖片／影片素材（logo、demo 影片等，不進版控）
Pic/

.DS_Store

# Claude Code / Codex 的本機 session 設定（含機器專屬絕對路徑與個人 allowlist，
# 對他人無意義且會不斷變動；專案級規則若日後要共用再單獨解禁 settings.json）
.claude/

# 地理編碼查詢快取（geocode_verify.py）。
# 內含案件地址與座標，絕不可入版控。data/*.csv 已被上面的 *.csv 規則涵蓋，
# 但 .json 沒有 —— 2026-08-24 加快取時發現這個缺口。
data/*.json
data/.geocode_cache*

# ── 截圖與狀況回報資料夾（2026-08-27）──────────────────────
# 起因：回報 bug 時把 Render Environment 頁面的截圖放進專案目錄，
# 而那張圖裡 GOOGLE_MAPS_API_KEY / ABUSEIPDB_API_KEY / WEB_AUTH_PASS
# 四個值全部明文可見。截圖看起來人畜無害，但它會把畫面上的一切
# （金鑰、案件地圖、對象姓名）原封不動帶進版控，而且 git 歷史刪不乾淨。
#
# 規則刻意只擋「根目錄的圖片」與「螢幕截圖命名樣式」——
# frontend/assets/ 底下的正式素材（logo.png）不受影響。
狀況資料夾/
/*.png
/*.jpg
/*.jpeg
截圖*.png
Screenshot*.png
螢幕截圖*.png
